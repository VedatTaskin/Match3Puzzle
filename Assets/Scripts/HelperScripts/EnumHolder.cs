public enum GamepieceType
{
    Normal,
    Special
}

public enum TileType
{
    Normal,
    Breakable,
    Nonbreakable,
}

public enum SpecialGamepieceType
{
    Stripped,
    Wrapped,
    Coloured,
}
